if 4 < 3 then 5 else 12

(* if statements are expressions, they have a "type" of what they return, in this case it is an int *)
(* also always use else, since if statements are expressions, they should always have a value *)